<a href="<%= user_path(current_user) %>" >
   <%= image_tag 'rails.png'  %>
</a>
<h1>
  current_user.name
</h1>
<span>
  <%= link_to 'view my profile', current_user %>
</span>
<span>
  <%= pluralize(current_user.posts.count, 'micropost') %>
</span>