<li>
  <%= image_tag 'rails.png', size: 52 %>
  <span style="vertical-align: 140%;">
  <%= link_to user.name, user %>

  <% if current_user.admin? && !current_user?(user) %>
   | <%= link_to 'delete', user, method: :delete, confirm: "You sure?" %>
  <% end  %>
  </span>
</li>