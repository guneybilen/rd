<!--  say_span StaticPagesHelper'da defined
 ama burda call edebiliyorsun ve problemsiz
 calisiyor.
-->

<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <!-- image_tag "rails.png", :class =>"gravatar" -->
        <%= @user.name %>
      </h1>
    </section>
    <section>
      <%= render 'shared/stats' %>
    </section>
  </aside>
  <div class="span8">
    <%= render 'follow_form' if signed_in? %>
    <% if @user.posts.any? %>
      <ol class="posts">
      <h3>Posts (<%= @user.posts.count %>)</h3>
        <%= render @posts %>
      </ol>
      <%= will_paginate @posts %>
    <% end %>
  </div>
</div>

