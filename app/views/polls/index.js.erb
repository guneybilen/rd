<% unless @poll_posts.empty? %>
  <% if @result != true %>
    $('h3.post-feed').hide();
    $('div.poll')
          .html("New Posts added." + "<a href='' id='poll'> Click Here!</a>")
          .addClass("alert alert-info")
          .click(function(){
              $('.pre-posts')
                     .prepend('<%= j render( partial: "polls/polls", locals: { :@poll_posts => @poll_posts }) %>');
          });
//  $('h3.post-feed').show();
  <% end %>
<% end %>
